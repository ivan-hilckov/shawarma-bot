# 🥙 Шаурма Бот

Профессиональный Telegram-бот для заказа шаурмы с полным функционалом корзины, системой заказов, уведомлениями персонала и автоматическим деплоем. Проект реализован на TypeScript с использованием современных практик разработки.

## 🎯 Ключевые особенности (ОБНОВЛЕНО!)

- ✅ **Полный функционал заказов** - корзина, оформление, история
- ✅ **Уведомления персонала** - автоматические оповещения о заказах
- ✅ **Фотографии блюд** - визуальное представление товаров
- ✅ **Архитектурные улучшения** - логирование, валидация, DI
- ✅ **Оптимизация производительности** - устранены N+1 запросы
- ✅ **CI/CD Pipeline** - автоматический деплой на VPS
- ✅ **Качество кода** - ESLint, Prettier, 154 теста
- ✅ **Production-ready** - Docker, health checks, мониторинг

## 📋 Текущий функционал (MVP)

- **Команда /start** - приветствие и главное меню
- **Выбор категорий:** 🌯 Шаурма (12 позиций), 🥤 Напитки (3 позиции)
- **Расширенное меню шаурмы:**
  - Шаурма Вегетарианская (большая/стандарт)
  - Шаурма классик (двойная/большая/маленькая)
  - Шаурма в сырном лаваше (большая/стандарт)
  - Цезарь-Ролл (большой/стандарт)
  - Хот-Доги (с сосиской/с курицей)
  - Гирос
- **Просмотр товаров** с ценами и описанием
- **🛒 Корзина покупок** - полный функционал с Redis
  - Добавление товаров в корзину
  - Изменение количества товаров (➕/➖)
  - Удаление товаров из корзины
  - Просмотр корзины с общей суммой
  - Очистка корзины
- **📦 Система заказов** - сохранение в PostgreSQL

  - Оформление заказов из корзины
  - Сохранение пользователей и заказов в БД

  - Просмотр истории заказов
  - Детальная информация о заказах
  - Статусы заказов (в ожидании, подтвержден, готовится, готов, доставлен)

- **🔔 Уведомления персонала** - автоматические оповещения (НОВОЕ!)
  - Уведомления о новых заказах в отдельный канал/группу
  - Кнопки быстрого управления заказами для персонала
  - Изменение статусов заказов администраторами
  - Уведомления об изменении статусов
  - Просмотр деталей заказов
  - Проверка прав доступа администраторов
- **Базовая навигация** с кнопками
- **Информация о заведении**

## 🚀 Быстрый запуск

> **🎉 Бот уже запущен и работает!**  
> Найдите [@SaurmaOtRayhanaBot](https://t.me/SaurmaOtRayhanaBot) в Telegram и отправьте `/start`

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка бота

1. Создайте бота у [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Создайте файл `.env`:

```bash
# Для локальной разработки (.env)
BOT_TOKEN=ваш_токен_от_BotFather
NODE_ENV=development
REDIS_URL=redis://redis:6379
DATABASE_URL=postgresql://shawarma_user:shawarma_pass@postgres:5432/shawarma_db

# Для продакшена (.env.production)
BOT_TOKEN=ваш_продакшен_токен
NODE_ENV=production
REDIS_URL=redis://redis:6379
DATABASE_URL=postgresql://shawarma_user:shawarma_pass@postgres:5432/shawarma_db

# Параметры для автоматического деплоя
DEPLOY_SERVER_HOST=5ee5d87f4ecb.vps.myjino.ru
DEPLOY_SERVER_PORT=49337
DEPLOY_SERVER_USER=root
DEPLOY_SERVER_PATH=~/shawarma-bot
```

### 3. Запуск

#### 🐳 Docker (рекомендуется)

```bash
# Быстрый старт с PostgreSQL, Redis и pgAdmin
# 1. Создайте .env файл с токеном бота:
echo "BOT_TOKEN=ваш_токен_от_BotFather" > .env
echo "NODE_ENV=production" >> .env
echo "REDIS_URL=redis://redis:6379" >> .env
echo "DATABASE_URL=postgresql://shawarma_user:shawarma_pass@postgres:5432/shawarma_db" >> .env

# Опционально: добавьте параметры деплоя для автоматизации
echo "DEPLOY_SERVER_HOST=your-server.com" >> .env
echo "DEPLOY_SERVER_PORT=22" >> .env
echo "DEPLOY_SERVER_USER=root" >> .env
echo "DEPLOY_SERVER_PATH=~/shawarma-bot" >> .env

# 2. Запустите все сервисы
npm run docker:up

# 3. Проверьте логи бота
npm run docker:logs

# Сервисы будут доступны на портах:
# - PostgreSQL: localhost:5433
# - Redis: localhost:6380
# - pgAdmin: localhost:8080
```

#### 💻 Локальная разработка

```bash
# Разработка (с TypeScript)
npm run dev

# Продакшн (скомпилированный JavaScript)
npm run build
npm start
```

#### 🧪 Тестирование

```bash
# Запуск всех тестов
npm test

# Запуск тестов с покрытием кода
npm test -- --coverage

# Запуск тестов в режиме наблюдения
npm run test:watch

# Отладка (с возможностью подключения debugger)
npm run debug

# Отладка с остановкой на первой строке
npm run debug:brk

# Отладка скомпилированного кода
npm run debug:prod
```

Бот готов к работе! Найдите его в Telegram и отправьте `/start`.

📖 **Документация:**

- [🚀 Быстрый старт с Docker](./QUICKSTART.md)
- [🐳 Подробная Docker документация](./DOCKER.md)
- [📋 Отчет о настройке Docker](./DOCKER_SETUP.md)
- [🐛 Руководство по отладке](./DEBUG.md)

## 🛠️ Технологический стек

- **Runtime:** Node.js
- **Language:** TypeScript (строгая типизация)
- **Telegram API:** node-telegram-bot-api (^0.66.0)
- **Database:** PostgreSQL 15 (в Docker, порт 5433)
- **Cache/Sessions:** Redis 7 (для корзины покупок, порт 6380)
- **Containerization:** Docker + Docker Compose
- **Environment:** dotenv для переменных окружения
- **Build:** TypeScript Compiler (tsc)
- **Dev Tools:** ts-node для разработки
- **Admin Panel:** pgAdmin 4 (веб-интерфейс для БД)
- **AI Integration:** Model Context Protocol (MCP) для доступа к PostgreSQL, Docker и Node.js отладки

## 📁 Структура проекта

```
food/
├── src/                      # TypeScript исходники
│   ├── bot.ts               # Главный файл бота
│   ├── handlers.ts          # Обработчики команд и сообщений
│   ├── notifications.ts     # Сервис уведомлений персонала (НОВОЕ!)
│   ├── database.ts          # Работа с PostgreSQL
│   ├── cart.ts              # Работа с корзиной Redis
│   ├── menu.ts              # Данные меню и функции работы с ними
│   ├── config.ts            # Конфигурация и переменные окружения
│   └── types.ts             # TypeScript типы и интерфейсы
├── assets/                   # Медиа файлы (НОВОЕ!)
│   ├── XXL.jpeg             # Фотографии блюд
│   ├── XXL (1).jpeg         # 8 изображений шаурмы
│   └── ...                  # в случайном порядке
├── dist/                     # Скомпилированный JavaScript (автогенерация)
├── __tests__/                # Тесты
├── Dockerfile                # Docker образ для бота
├── docker-compose.yml        # Оркестрация сервисов
├── init.sql                  # Инициализация PostgreSQL
├── docker.env.example        # Пример переменных для Docker
├── package.json              # Зависимости и скрипты
├── tsconfig.json             # Конфигурация TypeScript
├── jest.config.js            # Конфигурация тестов
├── .env                      # Переменные окружения (создать вручную)
├── .gitignore                # Исключения для Git
├── menu.csv                  # Исходные данные меню
├── README.md                 # Документация
├── DOCKER.md                 # Docker документация
└── PLAN.md                   # План развития
```

## 🔧 Требования

- Node.js (версия 14 или выше)
- npm или yarn
- Telegram Bot Token от [@BotFather](https://t.me/BotFather)

## 🎯 План развития

### ✅ Фаза 1: MVP + TypeScript + Docker (Текущая версия)

- [x] Базовая структура проекта
- [x] Команда /start и главное меню
- [x] Просмотр категорий (Шаурма/Напитки)
- [x] Показ товаров с ценами
- [x] Базовая навигация
- [x] **Миграция на TypeScript**
- [x] **Строгая типизация всех компонентов**
- [x] **Настройка сборки и разработки**
- [x] **🐳 Docker контейнеризация**
- [x] **🐘 PostgreSQL в Docker**
- [x] **🔧 pgAdmin веб-интерфейс**
- [x] **🛒 Redis для корзины покупок**
- [x] **📦 Полный функционал корзины**
  - [x] Добавление товаров
  - [x] Изменение количества
  - [x] Удаление товаров
  - [x] Просмотр и очистка корзины
  - [x] Подсчет общей суммы
- [x] **🗄️ Система заказов в PostgreSQL**
  - [x] Оформление заказов
  - [x] Сохранение в БД
  - [x] История заказов
  - [x] Детали заказов

### 🚀 Фаза 2: Интеграция с БД (Завершена)

- [x] Подключение к PostgreSQL
- [x] Система заказов в БД
- [x] Сохранение пользователей
- [x] История заказов

### 🎨 Фаза 3: Улучшения UX (В процессе)

- [x] Фотографии блюд (для шаурмы)
- [ ] Избранные товары
- [ ] Промокоды и скидки

### 🏢 Фаза 4: Админ-панель (Запланировано)

- [ ] Web-интерфейс для управления
- [ ] Статистика заказов
- [ ] Управление меню

### 💳 Фаза 5: Интеграция платежей (Запланировано)

- [ ] Подключение платежного шлюза
- [ ] Безопасная обработка транзакций
- [ ] История платежей

## 📝 Текущее состояние

### ✅ Реализовано (ОБНОВЛЕНО!):

- ✅ **TypeScript** - полная типизация проекта
- ✅ **Базовый функционал** - просмотр меню и навигация
- ✅ **Сборка и разработка** - настроенный workflow
- ✅ **Очистка проекта** - удалены старые JS файлы
- ✅ **Обработка ошибок** - улучшена стабильность callback'ов
- ✅ **TypeScript тесты** - расширенное покрытие тестами (108 тестов)
- ✅ **Покрытие кода** - 100% для handlers, menu, config модулей
- ✅ **🐳 Docker** - полная контейнеризация (протестировано)
- ✅ **🐘 PostgreSQL** - готовая БД с инициализацией (протестировано)
- ✅ **🔧 pgAdmin** - веб-интерфейс для управления БД
- ✅ **🤖 MCP PostgreSQL** - настроен доступ к БД через Model Context Protocol
- ✅ **🐋 MCP Docker** - настроено управление Docker контейнерами через AI
- ✅ **🐛 MCP Node.js Debugger** - настроена отладка Node.js приложений через AI
- ✅ **🛒 Redis корзина** - полный функционал корзины покупок
  - ✅ Добавление товаров в корзину
  - ✅ Изменение количества (➕/➖)
  - ✅ Удаление товаров
  - ✅ Просмотр корзины с суммой
  - ✅ Очистка корзины
  - ✅ TTL для автоочистки (1 час)
  - ✅ Полное покрытие тестами (92.75% statements)
- ✅ **🗄️ PostgreSQL заказы** - полная система заказов
  - ✅ Создание заказов из корзины
  - ✅ Сохранение пользователей в БД
  - ✅ История заказов пользователя
  - ✅ Детальная информация о заказах
  - ✅ Статусы заказов
  - ✅ Транзакции для целостности данных
  - ✅ Полное покрытие тестами (97.26% statements)
- ✅ **🔔 Уведомления персонала** - система оповещений (НОВОЕ!)
  - ✅ Автоматические уведомления о новых заказах
  - ✅ Кнопки быстрого управления заказами
  - ✅ Изменение статусов администраторами
  - ✅ Уведомления об изменениях статусов
  - ✅ Проверка прав доступа
  - ✅ Полное покрытие тестами (94.54% statements)
- ✅ **🖼️ Фотографии блюд** - визуальное представление товаров (ОБНОВЛЕНО!)
  - ✅ Добавлены фотографии для всех 12 позиций шаурмы
  - ✅ Автоматическая отправка фото при выборе товара
  - ✅ Индикация товаров с фото в меню (📸)
  - ✅ Проверка существования файлов перед отправкой
  - ✅ Fallback на текстовое сообщение при ошибках
  - ✅ **ИСПРАВЛЕНО:** Корректная работа путей к изображениям в Docker контейнере
  - ✅ **ИСПРАВЛЕНО:** Пересборка контейнера для обновления кода отправки фото

### ✅ Завершенные задачи:

- ✅ **Docker** → полная контейнеризация готова
- ✅ **PostgreSQL** → БД настроена и интегрирована с кодом
- ✅ **Redis корзина** → полный функционал реализован
- ✅ **PostgreSQL заказы** → система заказов реализована
- ✅ **Тесты** → полное покрытие TypeScript тестами (110 тестов)
- ✅ **Интеграция с БД** → подключение кода к PostgreSQL завершено
- ✅ **Уведомления персонала** → полная система уведомлений реализована
- ✅ **Фотографии блюд** → добавлены изображения для всех позиций шаурмы (исправлена работа в Docker)
- ✅ **🏗️ Архитектурные улучшения** → внедрены новые модули (НОВОЕ!)
  - ✅ Централизованная система логирования
  - ✅ Dependency injection через ServiceRegistry
  - ✅ Middleware для валидации входных данных
  - ✅ Rate limiting для защиты от спама
  - ✅ Улучшенная обработка ошибок
- ✅ **⚡ Оптимизация производительности** → устранены узкие места (НОВОЕ!)
  - ✅ Исправлена N+1 проблема в database queries
  - ✅ Настроен connection pooling для PostgreSQL
  - ✅ Оптимизированы SQL запросы
- ✅ **🚀 CI/CD Pipeline** → автоматизация деплоя (НОВОЕ!)
  - ✅ GitHub Actions для тестирования и деплоя
  - ✅ Автоматический деплой на VPS при коммите в master
  - ✅ Health checks для контейнеров
  - ✅ Уведомления в Telegram о статусе деплоя
- ✅ **🔧 Улучшенный скрипт деплоя** → поддержка нестандартных портов (ОБНОВЛЕНО!)
  - ✅ Поддержка SSH портов (не только 22)
  - ✅ Автоматическое чтение параметров из .env
  - ✅ Fallback на ручной ввод если .env недоступен
  - ✅ Корректная работа с scp и ssh через нестандартные порты
- ✅ **📋 Качество кода** → инструменты для поддержания качества (НОВОЕ!)
  - ✅ ESLint с правилами безопасности
  - ✅ Prettier для форматирования кода
  - ✅ Pre-commit hooks с lint-staged
  - ✅ Расширенные тесты (130+ тестов)

## 🔔 Реализация уведомлений персонала (Завершено ✅)

### 📋 Пошаговый план реализации

#### Шаг 1: Обновление типов данных ✅

- [x] Добавить `NOTIFICATIONS_CHAT_ID` и `ADMIN_USER_IDS` в интерфейс `Config`
- [x] Обновить `src/types.ts`

#### Шаг 2: Обновление конфигурации ✅

- [x] Добавить новые переменные окружения в `src/config.ts`
- [x] Обновить `docker.env.example` с примерами

#### Шаг 3: Создание сервиса уведомлений ✅

- [x] Создать `src/notifications.ts`
- [x] Реализовать класс `NotificationService` с методами:
  - [x] `notifyNewOrder()` - уведомление о новом заказе
  - [x] `notifyStatusChange()` - уведомление об изменении статуса
  - [x] `isAdmin()` - проверка прав администратора
  - [x] `formatNewOrderMessage()` - форматирование сообщения о заказе
  - [x] `getOrderManagementKeyboard()` - клавиатура управления заказом

#### Шаг 4: Интеграция с обработчиками ✅

- [x] Обновить `src/handlers.ts`:
  - [x] Интегрировать уведомления в `handleCheckout()`
  - [x] Добавить `handleAdminOrderAction()` для управления заказами
  - [x] Добавить импорт сервиса уведомлений

#### Шаг 5: Обновление основного файла бота ✅

- [x] Обновить `src/bot.ts`:
  - [x] Создать экземпляр `NotificationService`
  - [x] Добавить обработку admin callback'ов
  - [x] Экспортировать сервис для handlers

#### Шаг 6: Добавление тестов ✅

- [x] Создать `__tests__/notifications.test.ts`
- [x] Протестировать все методы `NotificationService`
- [x] Добавить тесты для admin handlers

#### Шаг 7: Обновление документации ✅

- [x] Добавить инструкции по настройке уведомлений
- [x] Обновить список функционала
- [x] Добавить примеры использования

#### Шаг 8: Тестирование и запуск ✅

- [x] Запустить все тесты: `npm test`
- [x] Проверить покрытие кода
- [x] Запустить Docker: `npm run docker:up`

### 🎯 Результат реализации

После выполнения всех шагов будет реализована полная система уведомлений:

- ✅ **Автоматические уведомления** о новых заказах в канал персонала
- ✅ **Кнопки управления** для быстрого изменения статусов заказов
- ✅ **Уведомления об изменениях** статусов для всего персонала
- ✅ **Проверка прав доступа** - только администраторы могут управлять заказами
- ✅ **Детальная информация** о заказах по запросу
- ✅ **Полное покрытие тестами** для надежности

### 🚀 Настройка после реализации

1. **Создайте канал/группу** для персонала ресторана
2. **Добавьте бота** в канал как администратора
3. **Получите ID канала** через [@userinfobot](https://t.me/userinfobot)
4. **Получите ID администраторов** через [@userinfobot](https://t.me/userinfobot)
5. **Добавьте в .env файл:**
   ```bash
   NOTIFICATIONS_CHAT_ID=-1001234567890
   ADMIN_USER_IDS=123456789,987654321
   ```

## 🔄 Следующие шаги

### 🚀 Для локальной разработки:

1. **🐳 Запустите Docker** - `npm run docker:up`
2. **🍽️ Изучите новое меню** - 12 позиций шаурмы от 190₽ до 350₽
3. **🛒 Протестируйте корзину** - добавляйте товары, изменяйте количество
4. **📦 Оформите заказ** - полный цикл от корзины до сохранения в БД
5. **📋 Просмотрите историю** - проверьте "Мои заказы" и детали заказов

### 🌐 Для деплоя на VPS:

1. **📖 Изучите инструкцию** - [DEPLOYMENT.md](./DEPLOYMENT.md)
2. **🔑 Настройте SSH ключи** для доступа к серверу
3. **⚙️ Настройте переменные деплоя** в `.env.production` (рекомендуется):

   ```bash
   # Создайте .env.production для продакшена
   cp docker.env.example .env.production

   # Добавьте параметры деплоя
   echo "DEPLOY_SERVER_HOST=your-server.com" >> .env.production
   echo "DEPLOY_SERVER_PORT=22" >> .env.production
   echo "DEPLOY_SERVER_USER=root" >> .env.production
   echo "DEPLOY_SERVER_PATH=~/shawarma-bot" >> .env.production

   # Укажите реальный BOT_TOKEN
   nano .env.production
   ```

4. **🚀 Запустите настройку сервера** - `./setup-server.sh`
5. **📤 Выполните деплой** - `./deploy.sh`
6. **📊 Мониторьте логи** через `./health-check.sh`

> **💡 Совет:** Если переменные деплоя настроены в `.env`, скрипт `./deploy.sh` будет использовать их автоматически, иначе запросит ввод вручную.

### 🔧 Быстрое исправление проблем SSH

Если возникают проблемы с доступом к GitHub на сервере:

```bash
# Автоматическое исправление SSH ключей для GitHub
./fix-github-ssh.sh
```

Этот скрипт:

- Создаст SSH ключ на сервере
- Покажет публичный ключ для добавления в GitHub
- Проверит подключение к GitHub
- Склонирует репозиторий если нужно

### 📁 Приоритет файлов окружения

Скрипты деплоя ищут файлы в следующем порядке:

1. **`.env.production`** - приоритет для продакшена
2. **`.env`** - fallback для разработки
3. **`docker.env.example`** - базовый шаблон

> **💡 Рекомендация:** Используйте `.env.production` для продакшена и `.env` для локальной разработки.

## 🚀 Подготовка к деплою на сервер

> **💡 Краткая сводка проблем и решений:**
>
> **Проблемы:**
>
> - 🖼️ Изображения (8 файлов, 550KB) нужно перенести на сервер
> - 🗄️ Состояние БД с заказами должно сохраниться
> - ⚙️ Первичная настройка сервера требует автоматизации
>
> **Решения:**
>
> - ✅ **4 готовых скрипта** для полной автоматизации
> - ✅ **Простой деплой** одной командой `./deploy.sh`
> - ✅ **Безопасный перенос** данных с проверкой целостности
> - ✅ **Автоматические бэкапы** каждые 6 часов

### 📋 Чек-лист подготовки данных

#### 1. 🖼️ Перенос изображений на сервер

**Проблема:** Папка `assets/` с изображениями блюд (8 файлов, ~550KB) не находится в `.gitignore`, но может потребовать специальной обработки при деплое.

**Решение:**

```bash
# На локальной машине - создание архива изображений
tar --no-xattrs -czf assets-backup.tar.gz assets/

# Перенос на сервер через scp
scp assets-backup.tar.gz user@your-server:/path/to/project/

# На сервере - распаковка
cd /path/to/project
tar -xzf assets-backup.tar.gz
rm assets-backup.tar.gz
```

**Автоматизация в CI/CD:**

```yaml
# Добавить в .github/workflows/ci-cd.yml
- name: Ensure assets directory exists
  run: |
    ssh ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} << 'EOF'
      cd ${{ secrets.VPS_PROJECT_PATH }}
      
      # Создаем папку assets если её нет
      mkdir -p assets
      
      # Проверяем наличие изображений
      if [ ! -f "assets/XXL.jpeg" ]; then
        echo "⚠️ Assets missing - manual upload required"
        echo "Run: scp -r assets/ user@server:/path/to/project/"
      fi
    EOF
```

#### 2. 🗄️ Инициализация базы данных

**Текущее состояние:**

- ✅ `init.sql` автоматически создает таблицы и начальные данные
- ✅ Меню загружается из `init.sql` (15 позиций)
- ✅ Структура БД готова к работе

**Проверка инициализации:**

```bash
# На сервере - проверка состояния БД
docker exec shawarma-postgres psql -U shawarma_user -d shawarma_db -c "
SELECT
  (SELECT COUNT(*) FROM categories) as categories_count,
  (SELECT COUNT(*) FROM menu_items) as menu_items_count,
  (SELECT COUNT(*) FROM orders) as orders_count;
"
```

#### 3. 💾 Стратегия резервного копирования

**Автоматические бэкапы:**

```bash
# Создание скрипта бэкапа
cat > backup-script.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="/home/ubuntu/backups"
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_FILE="shawarma_db_backup_${DATE}.sql"

mkdir -p $BACKUP_DIR

# Создание бэкапа
docker exec shawarma-postgres pg_dump -U shawarma_user shawarma_db > "$BACKUP_DIR/$BACKUP_FILE"

# Сжатие бэкапа
gzip "$BACKUP_DIR/$BACKUP_FILE"

# Удаление старых бэкапов (старше 7 дней)
find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete

echo "✅ Backup created: $BACKUP_FILE.gz"
EOF

chmod +x backup-script.sh
```

**Настройка cron для автоматических бэкапов:**

```bash
# Добавление в crontab
crontab -e

# Бэкап каждый день в 2:00
0 2 * * * /path/to/project/backup-script.sh >> /var/log/backup.log 2>&1

# Бэкап каждые 6 часов (для продакшена)
0 */6 * * * /path/to/project/backup-script.sh >> /var/log/backup.log 2>&1
```

#### 4. 🔄 Восстановление данных

**Восстановление из бэкапа:**

```bash
# Остановка бота для безопасности
docker-compose stop bot

# Восстановление БД
gunzip -c backup_file.sql.gz | docker exec -i shawarma-postgres psql -U shawarma_user -d shawarma_db

# Проверка восстановления
docker exec shawarma-postgres psql -U shawarma_user -d shawarma_db -c "SELECT COUNT(*) FROM orders;"

# Запуск бота
docker-compose start bot
```

**Миграция данных между серверами:**

```bash
# На старом сервере - экспорт
docker exec shawarma-postgres pg_dump -U shawarma_user shawarma_db > migration_export.sql

# Перенос на новый сервер
scp migration_export.sql user@new-server:/path/to/project/

# На новом сервере - импорт
docker exec -i shawarma-postgres psql -U shawarma_user -d shawarma_db < migration_export.sql
```

### 🛠️ Скрипты автоматизации

Проект включает готовые скрипты для автоматизации всех процессов деплоя:

#### 1. 🚀 `setup-server.sh` - Первичная настройка сервера

Автоматически устанавливает Docker, клонирует проект, создает конфигурацию:

```bash
# Запуск на сервере
curl -sSL https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/master/setup-server.sh | bash
# или
wget -O - https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/master/setup-server.sh | bash
```

**Что делает:**

- ✅ Устанавливает Docker и Docker Compose
- ✅ Клонирует репозиторий
- ✅ Создает .env файл с интерактивным вводом
- ✅ Создает скрипты бэкапа и мониторинга
- ✅ Настраивает автоматические бэкапы (cron)

#### 2. 🚀 `deploy.sh` - Простой деплой (НОВОЕ!)

Обновляет код и изображения на сервере одной командой:

```bash
# Запуск с локальной машины
./deploy.sh
```

**Что делает:**

- ✅ Проверяет SSH подключение
- ✅ Создает архив изображений (если есть)
- ✅ Загружает изображения на сервер
- ✅ Обновляет код через git pull
- ✅ Пересобирает и перезапускает контейнеры
- ✅ Проверяет работоспособность

#### 3. 📸 `transfer-assets.sh` - Только изображения

Отдельный перенос изображений (если нужно):

```bash
./transfer-assets.sh
```

#### 4. 🔄 `migrate-data.sh` - Миграция между серверами

Полная миграция при смене сервера:

```bash
./migrate-data.sh
```

#### 2. Скрипт проверки состояния

```bash
# health-check.sh
#!/bin/bash

echo "🔍 Проверка состояния Шаурма Бота..."

# Проверка контейнеров
echo "📦 Статус контейнеров:"
docker-compose ps

# Проверка БД
echo "🗄️ Состояние базы данных:"
docker exec shawarma-postgres psql -U shawarma_user -d shawarma_db -c "
SELECT
  'Categories: ' || COUNT(*) FROM categories
UNION ALL
SELECT
  'Menu items: ' || COUNT(*) FROM menu_items
UNION ALL
SELECT
  'Orders: ' || COUNT(*) FROM orders
UNION ALL
SELECT
  'Users: ' || COUNT(*) FROM users;
"

# Проверка изображений
echo "🖼️ Изображения:"
if [ -d "assets" ]; then
    echo "Найдено файлов: $(ls assets/*.jpeg 2>/dev/null | wc -l)"
    ls -la assets/ 2>/dev/null || echo "Папка assets пуста"
else
    echo "❌ Папка assets не найдена"
fi

# Проверка логов
echo "📋 Последние логи бота:"
docker-compose logs --tail=10 bot
```

### 📋 Пошаговая инструкция деплоя

#### Шаг 1: Подготовка локальной машины

```bash
# 1. Создание архива изображений
tar --no-xattrs -czf assets-backup.tar.gz assets/

# 2. Создание бэкапа БД (если есть данные)
docker exec shawarma-postgres pg_dump -U shawarma_user shawarma_db > local_backup.sql
```

#### Шаг 2: Настройка сервера

```bash
# 1. Подключение к серверу
ssh user@your-server

# 2. Установка Docker (если не установлен)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# 3. Клонирование проекта
git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
cd YOUR_REPO
```

#### Шаг 3: Перенос данных

```bash
# 1. Загрузка изображений
scp assets-backup.tar.gz user@server:~/shawarma-bot/
ssh user@server "cd ~/shawarma-bot && tar -xzf assets-backup.tar.gz"

# 2. Настройка окружения
cp docker.env.example .env
nano .env  # Добавить BOT_TOKEN и другие настройки
```

#### Шаг 4: Запуск и проверка

```bash
# 1. Запуск сервисов
docker-compose up -d

# 2. Проверка логов
docker-compose logs -f bot

# 3. Проверка БД
docker exec shawarma-postgres psql -U shawarma_user -d shawarma_db -c "SELECT COUNT(*) FROM menu_items;"

# 4. Тест бота в Telegram
# Отправить /start боту
```

### 🔧 Troubleshooting

#### Проблема: Конфликт портов PostgreSQL/Redis

Если на сервере уже запущен PostgreSQL или Redis:

```bash
# Ошибка: "address already in use"
# Решение: используются альтернативные порты
# PostgreSQL: 5433 вместо 5432
# Redis: 6380 вместо 6379

# Проверка занятых портов
sudo netstat -tlnp | grep :5432
sudo netstat -tlnp | grep :6379
```

#### Проблема: Предупреждения tar на macOS

Если видите сообщения типа "Ignoring unknown extended header keyword":

```bash
# Эти предупреждения безопасны и исправлены в скриптах
# Используется флаг --no-xattrs для подавления extended attributes macOS
# Автоматически определяется ОС и применяются нужные флаги
```

#### Проблема: Изображения не отображаются

```bash
# Проверка наличия файлов
ls -la assets/
# Проверка прав доступа
chmod 644 assets/*.jpeg
# Проверка в логах бота
docker-compose logs bot | grep -i "photo\|image\|asset"
```

#### Проблема: БД не инициализируется

```bash
# Пересоздание БД
docker-compose down -v
docker-compose up -d postgres
# Ждем инициализации
sleep 30
docker-compose up -d bot
```

#### Проблема: Потеря данных заказов

```bash
# Восстановление из бэкапа
docker-compose stop bot
gunzip -c latest_backup.sql.gz | docker exec -i shawarma-postgres psql -U shawarma_user -d shawarma_db
docker-compose start bot
```

### 📊 Мониторинг в продакшене

#### Настройка алертов

```bash
# Скрипт мониторинга
cat > monitor.sh << 'EOF'
#!/bin/bash
# Проверка работы бота
if ! docker-compose ps | grep -q "shawarma-bot.*Up"; then
    echo "❌ Бот не работает!" | mail -s "Shawarma Bot Alert" admin@example.com
fi

# Проверка места на диске
DISK_USAGE=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
if [ $DISK_USAGE -gt 80 ]; then
    echo "⚠️ Место на диске: ${DISK_USAGE}%" | mail -s "Disk Space Alert" admin@example.com
fi
EOF

# Добавление в cron (каждые 15 минут)
echo "*/15 * * * * /path/to/monitor.sh" | crontab -
```

### ✅ Финальный чек-лист готовности

#### 📋 Данные и изображения

- [ ] **Изображения**: Папка `assets/` с 8 файлами (550KB) перенесена на сервер
- [ ] **База данных**: `init.sql` создает таблицы и начальные данные (15 позиций меню)
- [ ] **Состояние заказов**: Существующие заказы сохранены и перенесены

#### 🔧 Инфраструктура

- [ ] **Бэкапы**: Настроены автоматические бэкапы каждые 6 часов
- [ ] **Мониторинг**: Скрипты проверки состояния настроены
- [ ] **Восстановление**: Процедуры восстановления протестированы
- [ ] **CI/CD**: GitHub Actions настроен для автоматического деплоя

#### 🚀 Автоматизация

- [ ] **Скрипты**: `setup-server.sh`, `transfer-assets.sh`, `migrate-data.sh` готовы
- [ ] **SSH ключи**: Настроены для автоматического доступа
- [ ] **Алерты**: Уведомления о проблемах настроены
- [ ] **Документация**: Все процедуры задокументированы

### 🎯 Быстрый старт деплоя

### 🎯 Упрощенный деплой

**Архитектура:**

- 🧪 **GitHub Actions** - только тестирование (CI)
- 🚀 **Локальный скрипт** - деплой через SSH

**Для нового сервера (2 команды):**

```bash
# 1. Настройка сервера (на сервере)
curl -sSL https://raw.githubusercontent.com/YOUR_USERNAME/YOUR_REPO/master/setup-server.sh | bash

# 2. Деплой (с локальной машины)
./deploy.sh
```

**Для обновления (1 команда):**

```bash
# Обновление кода и изображений
./deploy.sh
```

**Для миграции между серверами:**

```bash
# Полная миграция данных и изображений
./migrate-data.sh
```

**Результат:** Полная готовность к продакшен деплою с сохранением всех данных и изображений! 🚀

Шаурма Вегетарианская большая 270 ₽
Шаурма Вегетарианская стандарт 220 ₽
Шаурма классик двойная 350 ₽
Шаурма классик большая 270 ₽
Шаурма классик маленькая 220 ₽
Шаурма в сырном лаваше стандарт 230 ₽
Шаурма в сырном лаваше большая 280 ₽
Цезарь-Ролл большой 300 ₽
Цезарь-Ролл стандарт 250 ₽
Хот-Дог с сосиской В лаваше 190 ₽
Хот-Дог с сосиской или в булочке 190 ₽
Хот-Дог с курицей 220 ₽
Гирос 275 ₽
